# Cabin Focus — 개발 인계서

작성일: 2026-09-09
저장소: https://github.com/jban1001/planesimul
배포: https://jban1001.github.io/planesimul/

이 문서는 이어서 작업할 사람(또는 AI)에게 넘기기 위한 것이다. 코드를 읽으면 알 수 있는 것은 최소한으로 적고, **읽어도 모르는 것과 시행착오로 알아낸 것**을 중심으로 적었다.

---

## 1. 이게 뭔가

비행기 창가에 앉아 멍때리거나 공부하기 위한 정적 웹앱(PWA). 여행지를 고르면 인천에서 그곳까지 활주 → 이륙 → 상승 → 구름 통과 → 순항 → 하강 → 착륙이 이어진다.

**핵심 설계 결정: 창밖은 영상이 아니라 실시간 렌더링이다.** 프래그먼트 셰이더 한 장이 하늘·지형·공항·구름·날개를 전부 그린다.

원래는 Pexels 영상 클립을 이어붙이는 구조였다. 그러면 목적지 20곳 × 클립 7~9개 = 약 160개를 수집해야 하고, 클립 사이에 날개 모양·색감·고도가 튀고, 전환에 검은 화면이 끼고, 같은 영상이 반복된다. 렌더링으로 바꾸면서 그 문제가 전부 사라졌고, 오프라인 동작과 라이선스 문제도 같이 해결됐다.

**위성사진은 쓰지 않는다.** 구글 어스/맵은 약관상 타일 스크래핑과 외부 렌더러 사용이 금지된다. 합법적 소스(Esri, MapTiler, Sentinel-2)를 써도 위성사진은 수직 촬영이라, 고도 3m에서 옆으로 스치듯 보는 창가 시점에 투영하면 늘어져 뭉개진다. **사진이 가장 필요한 구간(활주·이륙·착륙)이 사진이 가장 안 먹히는 구간이다.** 대신 실제 활주로 형상 데이터를 쓴다(6절).

---

## 2. 파일 구조

```
dist/                      ← 배포되는 전부. 빌드 단계 없음
  index.html               화면 구조
  style.css                레이아웃, 테마, 반응형
  app.js                   여정 시계, 지도, 타이머, 소리, 목적지, 렌더러 구동
  skyview.js               창밖 렌더러 (WebGL 셰이더 + 블룸 파이프라인)
  journey.js               비행 프로파일, 바이옴, 태양, 기수 방위, 활주로 배치
  airports.js              21개 공항 실제 활주로 데이터 (생성물)
  sw.js                    오프라인 캐시
README.md                  사용자용
HANDOFF.md                 이 문서
```

의존성은 CDN의 MapLibre GL 하나뿐이다. npm도 번들러도 없다.

---

## 3. 좌표계 — 셰이더를 건드리기 전에 반드시 읽을 것

여기서 헷갈리면 모든 게 어긋난다.

- **+z 는 기수 방향**(진행 방향). 월드는 `uTravel`이 커지면서 +z로 흘러간다.
- **+x 는 오른쪽 날개 방향.** 카메라는 `uYaw = -1.35 rad`로 왼쪽 창을 본다. 즉 시선은 대략 **-x**, 약간 +z.
- **+y 는 위.** 카메라 높이는 `uAlt`(해발 m), 지면은 y=0.
- 지면 샘플 좌표 `p`는 `vec2(월드x, 월드z)`다. **즉 `p.y`는 z축이다.** 코드에서 `p.y`를 세로로 착각하면 안 된다.

이 좌표계의 중요한 귀결:

> 시선이 -x 방향이므로, **x는 화면상 깊이축**이다. x축으로 주기적인 패턴은 화면을 가로지르는 가로줄이 되고, z축으로 주기적인 패턴은 소실점으로 수렴하는 줄이 된다. 줄무늬 버그가 나오면 어느 축의 주기인지부터 보면 원인이 바로 좁혀진다.

태양은 **진방위(북 기준)로 계산한 뒤 기수 방위만큼 회전시켜** 이 좌표계에 넣는다(`journey.js`의 `sunVector`). 그래서 활주로 방위각이 조명에 직접 영향을 준다.

---

## 4. 렌더 파이프라인

프레임당 5번 그린다.

1. **씬** → half-float 프레임버퍼, 선형 HDR (톤매핑 전)
2. **브라이트 패스** → 1/4 해상도, 4탭 박스 다운샘플 + 임계 1.25 소프트 니
3. **블러** → 가로/세로 가우시안 2회 왕복 (핑퐁)
4. **합성** → 씬 + 블룸, ACES 톤매핑, 비네트, 디더링, 그레인

폴백 2단계: half-float이 없으면 8비트에 `uHdrScale = 6.0`으로 레인지를 눌러 담는다. 프레임버퍼 자체가 실패하면 `uDirect = 1`로 단일 패스(블룸 없이 셰이더 내부에서 톤매핑). `SkyView.create`가 `null`을 반환하면 app.js가 기존 Pexels 영상 폴백으로 넘어간다.

**성능 자동 조절**: 프레임 시간을 관찰해 내부 해상도(0.42~1.0배)와 구름 레이마칭 스텝(14~34)을 함께 조절한다.

> ⚠️ **90ms를 넘는 프레임은 튜닝에서 제외한다.** 탭이 가려지면 `requestAnimationFrame`이 스로틀되어 프레임 간격이 수백 ms가 되는데, 이걸 GPU 부하로 오해하면 사용자가 보지도 않는 동안 화질이 영구히 떨어진다. 실제로 RTX 5050에서 해상도가 55%까지 내려간 적이 있다.

---

## 5. 지상 LOD — 앨리어싱을 잡는 유일한 방법

`groundFootprint(t, height)`가 픽셀 하나가 지면에서 덮는 실제 크기를 미터로 돌려준다.

```
footprint ≈ (t² / 카메라높이) × (fov / 화면세로픽셀)
```

`resolve(피처크기, fp)`가 footprint보다 작은 피처를 끈다. 모든 지형·공항·마킹 디테일이 이걸 통과한다.

**거리 기반 페이드로는 절대 못 잡는다.** 고도 3m에서 50m 앞의 지면과 고도 11km에서 50km 앞의 지면은 "거리"만 비슷하지 픽셀당 덮는 넓이가 수백 배 다르다. 지면을 얕은 각도로 볼수록 세로 압축이 극심해지고, 그래서 활주 고도에서 **모든** 디테일이 모아레로 번진다. 이 함수가 그 문제의 해답이다.

새 지상 디테일을 추가할 때는 반드시 피처의 실제 크기를 `resolve()`에 넘겨라.

---

## 6. 공항

`dist/airports.js`는 [OurAirports](https://ourairports.com/data/)(퍼블릭 도메인)에서 생성했다. 공항별로 **가장 긴 활주로 하나만** 담는다. 장거리 노선이 실제로 쓰는 활주로다.

| 키 | ICAO | 활주로 | 방위 | 크기 |
|---|---|---|---:|---|
| seoul | RKSI | 16L/34R | 145° | 4000×60m |
| dubai | OMDB | 12R/30L | 121° | 4447×60m |
| paris | LFPG | 08L/26R | 85° | 4215×45m |

`headingDeg`는 낮은 번호 쪽 끝의 진방위. 지상 장면 전체를 활주로 정렬 좌표계에서 만들기 때문에 이게 기준이다.

**갱신 방법**: OurAirports의 `airports.csv`, `runways.csv`를 받아 가장 긴 활주로를 골라 같은 형식으로 다시 쓴다. `le_heading_degT`가 비어 있으면 양 끝 좌표로 대권 방위를 계산한다.

### 공항 배치의 함정

- 활주로는 `uRunwayZ0`(월드 z)에서 시작해 `uRunwayLength`만큼 이어진다. **유한하다.**
- 공항 부지(`uAirportZ0` ~ `uRunwayZ0 + 길이 + 500`)는 활주로보다 넓다. 유도로·계류장·터미널은 활주로가 아니라 **부지** 범위를 기준으로 마스킹해야 한다. 처음에 활주로 기준으로 했다가 택시 중에 공항이 통째로 사라졌다.
- 출발 활주로의 임계점은 `taxiDistance - 120`이다. 택시로 실제 거리(수 km)를 이동하기 때문에, 활주로를 원점에 두면 택시가 끝날 때쯤 활주로 끝을 지나 바다로 나간다.
- 도착 활주로는 `touchdownDistance - 340`에 둔다. `touchdownDistance`는 속도 프로파일을 수치 적분해 착륙 20초 전 위치를 미리 구한 값이다.
- **활주로 임계표시(피아노 건반) 위에서 시작하지 말 것.** 1.8m 간격 흰 줄 13개가 옆을 보는 시야를 가로질러 화면이 줄무늬로 뒤덮인다. 실제 라인업 지점인 120m 지점에서 시작한다.

---

## 7. 지형 릴리프

지면은 평면이 아니라 높이장이다. `reliefSolve()`가 뉴턴법 3회로 광선과 지형의 교점을 푼다(레이마칭보다 훨씬 싸다).

두 가지 안전장치가 필수다. 없으면 화면이 깨진다.

1. **스텝 클램프** — 수평에 가까운 광선은 분모가 0에 가까워져 뉴턴 스텝이 폭주한다. `clamp(step, -t0*0.6, t0*0.6)`, 결과도 `clamp(t, t0*0.35, t0*2.2)`.
2. **높이 천장** — 지형이 항공기보다 높으면 카메라가 지면 속에 들어가 솔버가 무너진다. `terrainH`가 `ceiling = max(uAlt*0.72, 30)`에 대해 `ceiling * (1 - exp(-h/ceiling))`로 부드럽게 눌러 담는다. 고도에 따라 릴리프를 아예 끄는 방식도 써봤지만, 그러면 취리히 접근에 알프스가 사라진다.

바이옴별 `relief` 스칼라가 높이를 조절한다(mountain 1.00, megacity 0.20).

---

## 8. 높이를 가진 물체

**이 프로젝트에서 "게임 같다"는 인상의 최대 원인은 세계에 높이를 가진 물체가 하나도 없다는 것이었다.** 눈높이 3m에서는 바닥에 그린 것이 전부 지평선에 붙어버려 아무것도 보이지 않는다.

현재 수직 구조물은 두 가지이고, 둘 다 **해석적 ray-plane 교차**로 그린다(레이마칭 없음, 매우 쌈).

- **터미널 피어** — `x = ±(활주로폭/2 + 370)`의 수직면. 유리창 밴드, 층 분할, 세그먼트별 높이 변화, 관제탑(62m), 야간 실내 조명. 길이 1450m로 유한하다.
- **주기된 항공기** — `x = ±(활주로폭/2 + 250)`의 수직면에 동체 실루엣.

`sceneT`가 깊이 테스트 역할을 한다. 지면 → 수직 구조물 → 구름 순으로 합성하며, 구름 마칭 상한(`cloudLimit`)도 여기서 온다.

**다음에 높이를 줄 만한 것**: 도시 건물(접근 구간에서 큰 효과), 나무 라인, 격납고. 같은 ray-plane 기법을 쓰면 된다.

---

## 9. 비행 프로파일 — 절대 시간이다

`journey.js`의 `DEPARTURE`/`ARRIVAL` 배열은 **초 단위**다. 비율이 아니다.

- `DEPARTURE`는 시작부터 2280초(38분)까지: 택시 11.5분 → 이륙 활주 40초 → 회전 → 상승
- `ARRIVAL`은 착륙 시점 기준 역산 2040초(34분): 강하 시작 → 접근 → 플레어 → 접지
- 순항이 그 사이 남는 시간을 전부 흡수한다

**왜 비율이면 안 되나**: 실제 ICN→CDG는 11시간 33분이다. 비율 프로파일이면 이륙 활주가 전체의 2%라 14분이 된다. 이륙 활주는 어떤 노선이든 40초다.

총 시간이 최소치(`MIN_JOURNEY_SEC`, 약 73분)보다 짧으면 양 끝 블록이 `blockScale()`로 함께 압축된다.

`bankAt`, `headingAt`, `crossfade`도 전부 절대 시간 기준이다. 새로 뭔가를 시간에 걸 때 비율을 쓰지 마라.

여정 길이는 `app.js`의 `journeyDurationFor()` — `거리/820km/h + 0.65시간`. 실제 비행 시간이다. 사용자가 공부하는 동안 계속 틀어두는 용도라 그렇게 정했다.

---

## 10. 바이옴과 크로스페이드

바이옴 9종(temperate, coastal, mediterranean, tropical, island, desert, mountain, northern, megacity). 각각 지면색·수역량·설량·도시밀도·구름량·운저고도·릴리프를 가진다.

**출발지 바이옴에서 목적지 바이옴으로 순항 중에 크로스페이드된다.** 태양(방위·고도·야간도)도 같이 넘어간다. 인천을 초록 해안으로 떠나 두바이에 모래로 닿는다. 이게 "여행하는 느낌"의 상당 부분을 만든다.

직접 입력 목적지는 위도로 바이옴을 추론하고(58° 초과 northern, 46° 초과 temperate, 23.5° 미만 tropical), 활주로 데이터가 없으므로 대권 항로의 역방위로 착륙한다.

---

## 11. 태양

`sunState(coords)`가 실제 천문 계산을 한다. 날짜에서 프랙셔널 이어 → 균시차와 적위 → 시간각 → 고도와 **진방위**. 방위가 필요한 이유는 3절 참조.

야간 판정은 고도 `smoothstep(0.5, -3.0, elevDeg)`. 별은 `smoothstep(0.55, 0.98, uNight)`로 게이팅한다 — 이걸 안 하면 해가 아직 떠 있는 노을에 별이 보인다.

---

## 12. 구름

적운층을 볼류메트릭 레이마칭한다(최대 34스텝, 지터링).

- 밀도는 노이즈를 **임계값으로 자른다**. 빼기로 하면 하늘 전체가 균일하게 흐려져 맑은 틈이 사라진다.
- 3D 값 노이즈 3옥타브로 침식한다. 2D로 하면 모든 높이에서 같게 깎여 솜뭉치가 된다. 얇은 가장자리일수록 세게 깎아 중심부는 남기고 윤곽만 부순다. 거리 26km 기준으로 게이팅(그 너머는 픽셀 이하).
- 조명은 태양 방향 2샘플 근사 + Henyey-Greenstein 위상함수. **위상함수는 반드시 클램프할 것** — 전방산란 피크가 그대로면 역광에서 구름이 날아간다.
- 지면에는 같은 필드로 구름 그림자가 진다.

`skyColor`의 Mie 산란 가중치도 낮게 유지해야 한다(현재 0.13). 높이면 저녁 역광에서 화면 전체가 하얗게 날아간다.

하늘 그라디언트는 **매우 압축되어 있다**(`pow(up, 0.16)`, `pow(up, 0.46)`). 창문은 하늘을 30° 정도밖에 안 보여주기 때문에, 완만한 그라디언트를 쓰면 보이는 영역이 전부 창백한 수평선 색이 된다.

---

## 13. GLSL 주의사항

- 타깃은 **WebGL1 / GLSL ES 1.00**이다. `half`, `fixed`, `double`은 **예약어**라 변수명으로 못 쓴다(`half`로 썼다가 컴파일이 깨졌다).
- 지역 배열의 동적 인덱싱 불가. 루프는 상수 경계 필요(`break`는 가능).
- `precision highp float`는 `GL_FRAGMENT_PRECISION_HIGH`로 가드되어 있다.
- 셰이더는 JS 문자열 배열로 되어 있다. 편집할 때 각 줄의 따옴표와 뒤의 `,`를 빠뜨리지 마라.

---

## 14. 테스트 방법

QA용 그리드 페이지를 임시로 만들어 쓰는 게 압도적으로 빠르다. `dist/_qa.html`에 캔버스 여러 개를 놓고 각각 다른 진행률로 한 프레임씩 렌더하면, 여정 전체를 스크린샷 한 장으로 본다. 태양 고도/방위를 URL 파라미터로 고정하면 시간대별 확인도 된다. **커밋 전에 반드시 지울 것.**

```bash
python -m http.server 4173 --directory dist
node --check dist/app.js && node --check dist/skyview.js && node --check dist/journey.js
```

회귀 체크리스트:

- 첫 장면이 활주로에 서 있는가, 터미널이 보이는가
- 고도가 단조 증가/감소하는가
- 목적지를 바꾸면 즉시 ICN/0%에서 재시작하는가
- 지도 비행기·ETA·게이지·고도·속도가 함께 움직이는가
- 지면에 줄무늬(모아레)가 없는가 — 특히 활주 고도
- 라이트/다크 모드에서 지도 노선이 보이는가
- 375px 화면에서 컨트롤이 겹치지 않는가
- OSM 저작자 표시가 가려지지 않았는가

---

## 15. 배포

```bash
git push origin main && git subtree push --prefix dist origin gh-pages
```

`main`에 소스, `gh-pages`에 `dist` 내용만 루트로. GitHub Pages 소스는 `gh-pages` 브랜치 / (root).

**정적 파일을 고쳤으면 세 곳을 함께 올린다**: `dist/sw.js`의 캐시 이름, `dist/sw.js`의 ASSETS 쿼리, `dist/index.html`의 `?v=`. 어긋나면 오래된 캐시가 남는다. 현재 v13.

서비스워커가 캐시 우선이라 **업데이트 후 첫 접속은 이전 버전이 뜨고 한 번 더 새로고침해야 최신이 된다.** 버전이 섞이지는 않으니 깨지지는 않는다.

---

## 16. 남은 작업

시각 품질:

- 도시 건물에 높이 주기 (8절 기법 재사용). 접근 구간에서 효과가 가장 클 것
- 지형에 도로망, 해안 절벽
- 구름 조명을 정식 라이트 마칭으로
- 비행운, 다른 항공기, 착륙등
- 날개 페어링이 아직 실루엣으로만 표현된다. 음영 로브를 더 다듬을 여지가 있음

기능:

- Windows EXE / Android APK 패키징 (현재 환경에 Flutter, JDK, ffmpeg 없음. Tauri나 Capacitor 필요)
- 집중 타이머와 여정 길이를 연동하는 옵션
- 사용자가 여정 속도(실시간 / 압축)를 고를 수 있게

알려진 제약:

- 지면은 단일 평면 높이장이다. 절벽, 동굴, 오버행 불가
- 공항은 활주로 1개 + 평행 유도로 1개로 단순화되어 있다
- 직접 입력 목적지는 활주로 데이터가 없다

---

## 17. 진행 기록

- [x] 기본 PWA, 두 화면 모드, 실제 지도, 여행지 검색
- [x] 영상 클립 방식 폐기, 실시간 WebGL 렌더링으로 전환
- [x] 진행률 기반 연속 여정 프로파일
- [x] 20개 목적지 바이옴 매핑, 직접 입력 위도 폴백
- [x] 출발지→목적지 지형·태양 크로스페이드
- [x] 볼류메트릭 구름, 3D 침식, 지형 법선 음영, 구름 그림자
- [x] 4패스 블룸 + ACES 톤매핑
- [x] 적응형 해상도/스텝 성능 조절
- [x] 실제 활주로 형상(OurAirports), 실제 태양 위치
- [x] 픽셀 풋프린트 기반 지상 LOD
- [x] 지형 릴리프, 수직 터미널·관제탑·주기 항공기
- [x] 절대 시간 프로파일 (실제 비행 시간)
- [x] GitHub Pages 공개 배포
- [ ] 도시 건물 높이
- [ ] EXE / APK 패키징
