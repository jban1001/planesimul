# Cabin Focus — Claude 작업 인계서

작성일: 2026-09-09  
작업 폴더: `C:\Users\JUN\Desktop\비행기멍때리기`

## 1. 사용자가 원하는 최종 결과

공부하거나 멍하니 바라볼 수 있는 비행기 창가 체험 앱이다.

- 사용자가 여행지를 고르면 인천(ICN)에서 해당 목적지로 출발한다.
- `WINDOW` 화면은 목적지 관광 영상이 아니라 승객이 비행기 창밖을 보는 시점이어야 한다.
- 한 노선은 `공항 활주 → 이륙 → 지상에서 멀어짐 → 구름 통과 → 구름 위 순항 → 목적지 접근 → 하강 → 착륙` 순서로 자연스럽게 이어져야 한다.
- 한 여정 안에서는 동일 영상을 반복하거나 루프하지 않는다.
- 영상 사이에는 짧은 교차 전환을 사용하고, 마지막 착륙 이후에는 멈춘다.
- `FLIGHT MAP`은 실제 지도 기반이며 영상 진행률, 비행기 위치, ETA, 게이지가 같은 진행 시계를 사용한다.
- 여행지를 중간에 바꾸면 기존 영상·타이머·비행기 위치를 모두 취소하고 ICN/0%부터 새 여정을 시작한다.
- 라이트/다크 모드를 모두 지원하며 두 테마 모두 노선이 잘 보여야 한다.
- 20개 기본 여행지와 사용자 직접 입력 목적지를 지원한다.
- 장기적으로 Windows 설치 파일과 Android 앱도 공유하고 싶어 한다.

## 2. 현재 구현 상태

정적 PWA 형태로 구현되어 있다.

- 앱 파일: `dist/index.html`, `dist/style.css`, `dist/app.js`
- PWA: `dist/manifest.webmanifest`, `dist/sw.js`
- 지도: MapLibre GL + OpenFreeMap/OpenStreetMap
- 직접 입력 위치 검색: Open-Meteo Geocoding API
- 창밖 대체 이미지: `dist/assets/window-view.png`
- Sites 설정: `.openai/hosting.json`
- 현재 비공개 배포 주소: `https://cabin-focus-jun.jban1001.chatgpt.site`
- 현재 Git HEAD: `c456a1d90146ce08fe0b2e19e5b5bfce82a3c19e`
- 작업 트리는 인계서 생성 전까지 깨끗한 상태였다.

현재 서비스 워커 캐시는 `cabin-focus-v8`, HTML의 CSS/JS 쿼리는 `?v=8`이다. 정적 파일을 수정하면 세 값을 함께 올려 오래된 캐시가 남지 않게 한다.

## 3. 창밖 화면 구현 방식 (2026-09-09 변경)

영상 클립을 이어붙이는 방식을 폐기하고 **실시간 렌더링**으로 전환했다.

`dist/skyview.js`가 WebGL 프래그먼트 셰이더 하나로 창밖 전체를 그린다.

- 하늘: 고도에 따라 천정이 깊어지는 다층 그라디언트 + Mie 전방산란 + 태양 원반 + 밤하늘 별
- 지형: 대륙/구릉/밭/숲/강/도시를 거리별 페이드로 합성하고, 높이장에서 유도한 법선으로 태양 음영 적용
- 공항: 활주로·갓길·유도로·계류장·터미널을 좌표 기반으로 그리며, 반경 밖에서는 일반 지형으로 넘어감
- 구름: 적운층을 실제 볼류메트릭 레이마칭(최대 34스텝)으로 그려 깊이·실루엣·은빛 테두리 생성. 3D 값 노이즈 3옥타브로 침식해 실루엣이 모든 축에서 흐트러진다(거리 26km 기준으로 게이팅). 지면에는 같은 필드로 구름 그림자를 드리움
- 지구 곡률: 고도에 따른 수평선 하강각 적용
- 날개: 스크린 공간 폴리곤 + 패널선·플랩선·항법등
- 후처리: 4패스 파이프라인. 씬을 half-float 프레임버퍼에 선형 HDR로 그린 뒤 → 1/4 해상도 브라이트 패스(임계 1.25, 소프트 니) → 가로/세로 가우시안 블러 2회 왕복 → 합성 시 ACES 톤매핑·비네트·디더링·그레인. half-float를 못 쓰면 8비트에 `uHdrScale`(6.0)로 레인지를 눌러 담고, 프레임버퍼 자체가 실패하면 `uDirect` 경로로 단일 패스 폴백

`dist/journey.js`가 여정 프로파일과 목적지 바이옴을 담당한다.

- `KEYS` 배열이 진행률 0~1에 대한 고도·피치·속도·진동 키프레임
- 모든 값이 보간되므로 이륙에서 착륙까지 **장면 전환이라는 개념 자체가 없다**
- 9개 바이옴 프리셋(temperate, coastal, mediterranean, tropical, island, desert, mountain, northern, megacity)
- 출발지(인천=coastal)에서 목적지 바이옴으로 진행률 0.16~0.86 구간에 걸쳐 크로스페이드. 목적지 현지 시각 기반 태양 위치도 함께 크로스페이드된다

## 4. 이 방식이 해결한 것

- 20개 노선 × 7~9개 클립 수집(약 160개) 작업이 **전부 불필요**해졌다
- 같은 영상 반복, 클립 간 색감/날개/고도 불일치, 검은 전환 화면 문제가 원천적으로 사라졌다
- 직접 입력 목적지도 위도 기반 바이옴 추론으로 즉시 커버된다
- 외부 영상 URL 의존이 없어 오프라인 동작이 가능하고, 대역폭과 라이선스 문제가 없다
- 정적 파일 총합이 작아 PWA 설치와 향후 EXE/APK 패키징이 가벼워진다

## 5. 성능

`SkyView`가 프레임 시간을 관찰해 내부 렌더 해상도(0.42~1.0배)와 구름 레이마칭 스텝 수(14~34)를 자동 조절한다.

주의: 탭이 가려지면 `requestAnimationFrame`이 스로틀되어 프레임 간격이 커진다. 이를 GPU 부하로 오해하지 않도록 90ms를 넘는 프레임은 튜닝에서 제외한다.

WebGL을 못 쓰는 환경에서는 `SkyView.create`가 `null`을 반환하고, 기존 Pexels 영상 폴백 경로(`setWindowVideo`)로 동작한다.

## 6. 목적지 바이옴 매핑

`dist/journey.js`의 `DESTINATION_BIOMES`에 20개 기본 목적지가 모두 매핑되어 있다. 새 목적지를 추가하려면 `DESTINATIONS`에 좌표·시간대를 넣고 `DESTINATION_BIOMES`에 바이옴 키 한 줄만 추가하면 된다.

## 7. 남은 시각 품질 개선 여지

- 지형에 도로망과 해안 절벽이 없다
- 이착륙 시 활주로가 z축으로 무한히 이어진다. 공항을 유한 영역으로 만들면 상승 중 공항이 뒤로 멀어지는 느낌이 살아난다
- 구름 조명이 태양 방향 2샘플 근사다. 정식 라이트 마칭으로 바꾸면 두꺼운 구름 내부 음영이 더 정확해진다
- 비행운, 다른 항공기, 착륙등 같은 디테일이 없다

## 8. 반드시 유지해야 하는 기존 기능

- 여행지 변경 즉시 ICN/0%에서 재시작
- ETA 초 단위 감소
- 게이지, 퍼센트, 지도 비행기 위치 동기화
- 비행 종료 시 100%에서 멈춤
- 라이트/다크 지도 노선 대비
- 실제 지도 확대/축소
- 25/50/90분 집중 타이머
- 기내 소음, 전체 화면, UI 숨김
- 직접 입력 여행지 검색
- PWA 설치
- 데스크톱/모바일 반응형 UI

## 9. 테스트 체크리스트

각 노선을 추가할 때 최소한 아래를 확인한다.

- 첫 장면이 활주/이륙 전 장면인가?
- 장면이 고도 순서에 맞게 이어지는가?
- 같은 영상이 다시 나오거나 루프하지 않는가?
- 장면 전환 중 검은 화면이 길게 나오지 않는가?
- 마지막이 실제 하강/착륙 장면인가?
- 마지막 이후 자동으로 첫 장면으로 돌아가지 않는가?
- `REPLAY`를 눌렀을 때 ICN/0%부터 다시 시작하는가?
- 재생 도중 다른 여행지를 선택하면 이전 영상이 다시 튀어나오지 않는가?
- 지도 비행기, ETA, 두 게이지가 모두 함께 진행되는가?
- 라이트/다크 모드에서 버튼과 노선이 보이는가?
- 390px 안드로이드 화면과 1920px Windows 화면에서 컨트롤이 겹치지 않는가?
- 영상 로딩 실패 시 `window-view.png` 폴백이 보이는가?

수정 후 기본 검사는 다음과 같다.

```powershell
node --check dist\app.js
node --check dist\sw.js
python -m http.server 4173 --directory dist
```

## 10. 배포 시 주의사항

- `.openai/hosting.json`에 기존 `project_id`가 있으므로 새 Sites 프로젝트를 만들지 않는다.
- Sites 관련 작업은 설치된 `sites-building`, `sites-hosting` 지침을 먼저 읽고 따른다.
- 소스를 검증한 뒤 Git에 커밋하고 기존 Sites 소스 저장소로 푸시한다.
- 저장소 토큰이나 단기 자격 증명을 파일·Git remote·로그에 남기지 않는다.
- 정적 패키지는 `dist/`와 `dist/.openai/hosting.json`을 포함해야 한다.
- Windows에서 공식 패키징 스크립트가 실패한 적이 있다. 그 경우 별도 staging 폴더에 `dist`를 복사하고 `dist/.openai/hosting.json`을 넣은 뒤 tar.gz로 만들었다.
- 배포가 성공하면 기존 브라우저 탭을 최종 주소로 넘긴다.
- 현재 사이트는 소유자 전용 비공개다. 공개 공유로 전환할 때는 Sites의 접근 권한 변경 및 공개 배포 절차를 따르고, 사용자에게 공개 범위를 명확히 알린다.

## 11. Windows EXE / Android 공유 방향

사용자에게는 우선 공개 PWA가 가장 적합하다.

- 정적 호스팅을 사용하면 사용자가 개인 서버를 운영할 필요가 없다.
- 하나의 공개 URL을 Windows와 Android에서 모두 열고 앱처럼 설치할 수 있다.
- EXE/APK는 오프라인 사용이나 별도 설치 파일이 꼭 필요할 때 2단계로 진행한다.

현재 개발 환경 확인 결과:

- Node.js/npm 설치됨
- Flutter 없음
- Java/JDK 없음
- ffmpeg 없음

따라서 바로 EXE/APK를 빌드하기 전에 배포 방식을 결정해야 한다.

- Windows: Electron은 만들기 쉽지만 용량이 크다. Tauri는 작지만 Rust/WebView2 빌드 환경이 필요하다.
- Android: Capacitor가 현재 웹 코드를 재사용하기 좋다. APK/AAB 빌드에는 Android Studio/JDK가 필요하다.
- 외부 지도·검색·영상 URL을 그대로 사용하면 EXE/APK여도 인터넷이 필요하다.
- 완전 오프라인으로 만들려면 영상과 지도 데이터를 포함해야 하므로 설치 파일이 매우 커진다.

## 12. 권장 작업 순서

토큰 중단에 대비하여 한 번에 모든 노선을 고치지 말고 다음 순서로 작게 커밋한다.

1. 도쿄 전용 코드를 범용 `CINEMATIC_JOURNEYS` 구조로 리팩터링하고 커밋
2. Paris, London, Zürich, Helsinki, Reykjavík 추가 후 커밋
3. New York, Los Angeles, Vancouver, Honolulu 추가 후 커밋
4. Rome, Barcelona, Istanbul, Cairo, Dubai 추가 후 커밋
5. Singapore, Bangkok, Bali 추가 후 커밋
6. Sydney, Auckland 추가 후 커밋
7. 사용자 직접 입력용 지역 프리셋 추가 후 커밋
8. 모바일/라이트/다크/목적지 전환 회귀 테스트
9. 캐시 버전 갱신 및 비공개 재배포
10. 사용자 확인 뒤 공개 PWA 또는 EXE/APK 배포 작업 진행

각 커밋 후 이 문서 맨 아래 진행 기록을 업데이트한다.

## 13. 진행 기록

- [x] 기본 PWA 및 두 화면 모드
- [x] 실제 지도와 여행지 검색
- [x] 여행지 변경 초기화 및 ETA/게이지/비행기 동기화
- [x] 라이트/다크 테마 개선
- [x] 도쿄 반복 없는 8장면 연속 여정 (영상 클립 방식, 이후 폐기)
- [x] 창밖을 실시간 WebGL 렌더링으로 전환 (`skyview.js`)
- [x] 여정을 진행률 기반 연속 프로파일로 일반화 (`journey.js`)
- [x] 20개 기본 목적지 전부 바이옴 매핑 완료
- [x] 직접 입력 목적지 위도 기반 바이옴 폴백
- [x] 출발지→목적지 지형·태양 크로스페이드
- [x] 볼류메트릭 구름, 지형 법선 음영, 구름 그림자, ACES 톤매핑
- [x] 적응형 해상도/스텝 성능 조절
- [ ] 공개 배포 (GitHub Pages 예정)
- [ ] Windows EXE 또는 Android APK 패키징

